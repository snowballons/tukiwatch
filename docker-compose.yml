services:
  api:
    image: ${TUKIWATCH_SERVER_IMAGE:-snowballons/tukiwatch-server:1.1.1}
    ports:
      - "8000:8000"
    environment:
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-*}
    env_file:
      - path: .env
        required: false
