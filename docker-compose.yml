services:
  api:
    build:
      context: ./api
    ports:
      - "8000:8000"
    environment:
      API_KEY: ${API_KEY:-}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-*}
    env_file:
      - path: .env
        required: false
    command: gunicorn main:app --host 0.0.0.0 --port 8000 --workers 2